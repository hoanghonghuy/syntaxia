import fs from 'node:fs'
import path from 'node:path'

const root='docs/curriculum/postgresql'
const expected=[['postgresql-types',0],['limiting-rows',1],['handling-null',2],['serial-identity',3],['boolean-filters',4],['text-operators',5],['date-basics',6],['returning-clause',10],['upsert-on-conflict',11],['jsonb-basics',12],['array-basics',13],['coalesce-nullif',14],['distinct-on',15],['indexes-intro',16],['window-functions',20],['common-table-expressions',21],['filter-clause',22],['explain-basics',23],['transactions-basics',24]]
const v2Migrated=new Set(expected.map(([slug])=>slug))
const mutationSlugs=new Set(['serial-identity','returning-clause','upsert-on-conflict','indexes-intro','transactions-basics'])
const mutationNeedsVerify=new Set(['serial-identity','upsert-on-conflict','indexes-intro','transactions-basics'])
function parse(md){const m=md.match(/^---\r?\n([\s\S]*?)\r?\n---/);if(!m)return{err:'no fm'};const fm=m[1],body=md.slice(m[0].length);const get=k=>{const r=fm.match(new RegExp(`^${k}:\\s*(.*)$`,'m'));return r?r[1].trim():''};const hb=fm.match(/exercise:\s*\n[\s\S]*?hints:\s*\n((?:\s{4}-.*\n?)+)/);return{id:get('id').replace(/[\"']/g,''),track:get('track').replace(/[\"']/g,''),locale:get('locale').replace(/[\"']/g,''),slug:get('slug').replace(/[\"']/g,''),order:Number(get('order')),canDo:get('can_do').replace(/^[\"']|[\"']$/g,''),published:get('published'),hintCount:hb?(hb[1].match(/^\s{4}-/gm)||[]).length:0,hasSolution:/\n\s{2}solution:/.test(fm),hasPreview:/\n\s{2}preview:/.test(fm),hasExpected:/\n\s{2}expected:/.test(fm),hasSeed:/^sandbox_seed:/m.test(fm),allowsMutations:/allow_mutations:\s*true/.test(fm),hasVerifySql:/verify_sql:\s*[\"']?SELECT\b/i.test(fm),leadingH1:/^\s*#\s+[^#]/m.test(body),body}}
const heading=(b,en,vi)=>new RegExp(`^##\\s+(?:${en}|${vi})\\s*$`,'im').test(b);const issues=[],parsed={en:{},vi:{}}
for(const loc of ['en','vi']){const dir=path.join(root,loc),files=fs.readdirSync(dir).filter(f=>f.endsWith('.md'));if(files.length!==19)issues.push(`${loc} PostgreSQL file count ${files.length}, want 19`);for(const f of files){const slug=f.replace(/\.md$/,''),p=parse(fs.readFileSync(path.join(dir,f),'utf8'));parsed[loc][slug]=p;if(p.err){issues.push(`${loc}/${f} ${p.err}`);continue}if(p.leadingH1)issues.push(`${loc}/${f} leading H1`);if(p.published!=='true')issues.push(`${loc}/${f} not published`)}for(const[slug,order]of expected){const p=parsed[loc][slug];if(!p){issues.push(`${loc} missing ${slug}`);continue}if(p.order!==order)issues.push(`${loc}/${slug} order ${p.order}, want ${order}`);if(!p.canDo)issues.push(`${loc}/${slug} missing can_do`);if(p.hintCount<3)issues.push(`${loc}/${slug} needs 3 progressive hints`);if(!p.hasSolution||!p.hasPreview||!p.hasExpected||!p.hasSeed)issues.push(`${loc}/${slug} incomplete exercise contract`);if(!heading(p.body,'Mental model','Mô hình tư duy'))issues.push(`${loc}/${slug} missing mental model`);if(!heading(p.body,'Predict before you run','Dự đoán trước khi chạy'))issues.push(`${loc}/${slug} missing prediction`);if(!heading(p.body,'Worked example','Ví dụ mẫu'))issues.push(`${loc}/${slug} missing worked example`);if(!heading(p.body,'Debug this','Tìm lỗi'))issues.push(`${loc}/${slug} missing debugging`);if(!heading(p.body,'Common mistakes','Lỗi thường gặp'))issues.push(`${loc}/${slug} missing mistakes`);if(!heading(p.body,'Your turn','Thử ngay'))issues.push(`${loc}/${slug} missing build task`);if(!heading(p.body,'Quick check','Tự kiểm tra'))issues.push(`${loc}/${slug} missing recall check`);if(!/\*\*(?:Answer|Đáp án):\*\*/.test(p.body))issues.push(`${loc}/${slug} quick check missing answer`);if(!/```(?:sql|text)[\s\S]*?```/i.test(p.body))issues.push(`${loc}/${slug} missing code example`);if(mutationSlugs.has(slug)&&!p.allowsMutations)issues.push(`${loc}/${slug} mutation missing allow_mutations`);if(mutationNeedsVerify.has(slug)&&!p.hasVerifySql)issues.push(`${loc}/${slug} mutation missing SELECT verify_sql`)}}
for(const[slug]of expected){const en=parsed.en[slug],vi=parsed.vi[slug];if(!en||!vi)continue;if(en.id!==vi.id)issues.push(`${slug} id mismatch`);if(en.track!==vi.track)issues.push(`${slug} track mismatch`);if(en.order!==vi.order)issues.push(`${slug} order mismatch`)}if(issues.length){console.log('FAIL');console.log(issues.join('\n'));process.exit(1)}console.log('PASS PostgreSQL: 19/19 lessons satisfy IT Learning V2 + parity + mutation gates')
